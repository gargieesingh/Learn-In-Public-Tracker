'use client'
import { useState } from 'react'
const targets = [{ label: 'Copy link', icon: '⧉', copyText: (url: string) => url, copiedLabel: 'Copied!' }, { label: 'Discord', icon: '◈', copyText: (url: string) => `📚 Follow my learning streak: ${url}`, copiedLabel: 'Copied for Discord!' }, { label: 'WhatsApp', icon: '◉', href: (url: string) => `https://wa.me/?text=${encodeURIComponent(`Check out my learning streak! ${url}`)}` }, { label: 'Twitter/X', icon: '𝕏', href: (url: string) => `https://twitter.com/intent/tweet?text=${encodeURIComponent("I’m learning in public! Follow my streak 🔥")}&url=${encodeURIComponent(url)}` }, { label: 'LinkedIn', icon: 'in', href: (url: string) => `https://linkedin.com/sharing/share-offsite/?url=${encodeURIComponent(url)}` }]
export function SharePanel({ url }: { url: string }) { const [copied, setCopied] = useState<string | null>(null); const copy = async (label: string, text: string) => { await navigator.clipboard.writeText(text); setCopied(label); window.setTimeout(() => setCopied(null), 1800) }; return <div className="flex flex-wrap gap-2">{targets.map((target) => target.href ? <a key={target.label} href={target.href(url)} target="_blank" rel="noreferrer" className="rounded-full border border-white/10 bg-white/5 px-3 py-2 text-xs font-semibold transition hover:scale-105 hover:border-mint"><span className="mr-1">{target.icon}</span>{target.label}</a> : <button key={target.label} onClick={() => void copy(target.label, target.copyText(url))} className="rounded-full border border-white/10 bg-white/5 px-3 py-2 text-xs font-semibold transition hover:scale-105 hover:border-mint"><span className="mr-1">{target.icon}</span>{copied === target.label ? target.copiedLabel : target.label}</button>)}</div> }
